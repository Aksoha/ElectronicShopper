@page "/"
@namespace ElectronicShopper.Pages
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@{
    Layout = "_Layout";
}
    @* HACK: render mode is currently set to server instead of prerender due to issues 
    which occured when CartService attempted to load local storage*@
<component type="typeof(App)" render-mode="Server" />
